spring:
  datasource:
    url: jdbc:h2:mem:studymatchmaker
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true

server:
  port: 8080

app:
  jwt:
    secret: ThisIsASecretKeyThatIsAtLeast32CharactersLong123!
    expiration-ms: 86400000